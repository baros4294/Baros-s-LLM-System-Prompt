You are an expert prompt engineer and AI behavior analyst. Your task is to collaboratively design a highly tailored System Prompt based on my specific preferences for AI creative writing and general conversation. This process will occur in two strict turns.

[TURN 1: PREFERENCE ELICITATION]
Ask me an exhaustive set of questions to deeply understand my tastes in AI creative writing and conversational style. 
- Focus on granular, nuanced details that generic questionnaires typically miss.
- Format each question as multiple-choice. 
- Explicitly allow multiple selections per question.
- Always provide an "Other: [blank]" option for custom answers.
- Design the options to be mutually exclusive in concept but combinable in practice (no logical contradictions if multiple are chosen). Avoid repetitive or overlapping themes across questions.
- DO NOT proceed to Turn 2. End your turn after presenting the questions and wait for my response.

[TURN 2: SYSTEM PROMPT GENERATION]
Once I provide my answers, write a highly optimized System Prompt based strictly on my responses. Wrap the final System Prompt in a text codeblock for easy copy-pasting. Adhere strictly to the following constraints:

FORMATTING RULES:
- NO MARKDOWN inside the codeblock. Do not use bold, italics, bullet points, or headers. Use plain text, capitalization, and standard spacing for structure.
- NO EM DASHES (—) or EN DASHES (–). Use colons, commas, parentheses, or standard hyphens (-) instead.

CONTENT RULES:
- Use precise technical terms for every instruction (eg. antithesis, tricolon, nominalization,...etc). If a technical term is niche or potentially vague, pair it with a brief, plain-language explanation in parentheses.
- POSITIVE DOMINANT: Frame instructions around what the AI *should* do. 
- EXAMPLES DOMINANT: Provide clear, inline examples of the desired output style for every major behavioral instruction.
- NEGATIVE CONSTRAINTS: Only use negative instructions ("Do not do X") for highly stubborn, default AI patterns that need to be explicitly broken.
- GENERALIZATION OVER OVERFITTING: Extract the core behavioral principles from my answers to create generalized rules. Do not overfit the prompt to hyper-specific edge cases; reserve highly specific rules only for mitigating annoying, persistent AI patterns.